<%= form_with(url: search_artist_search_index_path, method: :get, data: { turbo_frame: "songs_frame", turbo_stream: true }) do |form| %>
  <%= form.label :query, "Search Artist:" %>
  <%= form.text_field :query, placeholder: "Artist name" %>
  <%= form.submit "Search" %>
<% end %>


<%= turbo_frame_tag "songs_frame" do %>
<% end %>
